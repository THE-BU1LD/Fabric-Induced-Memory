experiment:
  name: fim_diffusion_test

  dataset:
    type: diffusion
    sequence_length: 50
    grid_size: 64

  evaluation:
    horizon: 20
    metrics:
      - mse
      - retention
      - recall