# Test Pack

Drop the `tests/` directory into the repository root and run:

```bash
pytest -q
```

Coverage:
- memory retrieval, consolidation, and persistent memory flow
- benchmark generation and rollout shapes
- long-horizon rollout stability
- fractional diffusion dynamics
- Lévy stable sampling and path generation
- training execution and checkpoint save/resume
